<% if user_signed_in? %>
 	<%= link_to('Profile', current_user, class: "btn btn-lg nav-link") %>
<% else %>
	<%= link_to('Register', new_user_registration_path, class: "btn btn-lg nav-link")  %>
<% end %>