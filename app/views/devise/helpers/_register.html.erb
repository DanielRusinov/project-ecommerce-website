<% if user_signed_in? %>
 	<%= link_to('Profile', current_user, class: "btn btn-default btn-lg" ) %>
<% else %>
	<%= link_to('Register', new_user_registration_path, class: "btn btn-default btn-lg")  %>
<% end %>